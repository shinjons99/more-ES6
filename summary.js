//* 1. optional chaining 

//* student.marks?.math

//* 2. map, filter, find

//* 3. (optional): forEach, reduce(advance)

//* 4. class

//* 5. (optional) inheritance , super

//* 6. (must) prototypical  Inheritance


const numbers = [1, 3, 5, 7, 8, 9];

const evenNumber = numbers.map(number => number % 2 === 0);
console.log(evenNumber);